{
  "//": "Type-check only (noEmit). esbuild remains the sole build tool. Scope is the UI bundle's entry graph -- the node-side CLI files (cli.ts, history-cli.ts, codex-connector.ts, diff.ts) are excluded because they need @types/node, which this check does not require.",
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": false,
    "noImplicitAny": false,
    "noEmit": true,
    "skipLibCheck": true,
    "types": []
  },
  "include": ["src/ui.ts", "src/icons.ts", "src/i18n.ts", "src/types.ts"]
}
